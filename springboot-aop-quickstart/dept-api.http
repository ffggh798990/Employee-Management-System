### 用 IDEA 自带的 HTTP Client 发请求
### 点每个请求左边的绿色三角就能发,不用装 Postman / Apifox
### 端口跟着 springboot-aop-quickstart 的 server.port 走,现在是 8080

### 1. 查询全部部门 —— 发完看 IDEA 控制台有没有 "方法执行耗时"
GET http://localhost:8080/depts

### 2. 根据 ID 查询
GET http://localhost:8080/depts/1

### 3. 新增部门 —— 这个最直观,切面会打印耗时
POST http://localhost:8080/depts
Content-Type: application/json

{
  "name": "测试部门"
}

### 4. 修改部门
PUT http://localhost:8080/depts
Content-Type: application/json

{
  "id": 1,
  "name": "学工部"
}

### 5. 删除部门(把 id 换成你要删的)
DELETE http://localhost:8080/depts?id=6
